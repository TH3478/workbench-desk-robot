{
  "issue": 48,
  "human_owner": "Quchaosheng",
  "objective": "在 ToolRegistry 注册边界原子地拒绝畸形工具 schema。",
  "allowed_paths": [
    "services/agent_runtime/workbench_agent_runtime/tool_registry.py",
    "tests/unit/test_tool_registry.py",
    "docs/task_packets/issue-48-tool-schema-registration.json"
  ],
  "read_only_paths": [
    "services/agent_runtime/workbench_agent_runtime/tool_schemas.py",
    "interfaces/**",
    "libs/contracts/**"
  ],
  "forbidden": [
    "firmware/**",
    "robot/control/**",
    "services/world_model/**"
  ],
  "acceptance": [
    "注册拒绝缺失与未知的 schema 键",
    "required_params 与 optional_params 是不相交的字符串集合",
    "每个允许参数恰好有一个显式支持的类型",
    "target_id_required 是 bool",
    "无效注册保持注册表不变",
    "每个被接受的 schema 都能在不抛出内部异常的情况下验证",
    "默认六工具注册通过"
  ],
  "commands": [
    "python -m pytest tests/unit/test_tool_registry.py -v",
    "make test",
    "make contract",
    "make context-check"
  ],
  "evidence": [
    "工具注册表回归测试输出",
    "完整测试输出",
    "contract 与 context 检查输出"
  ],
  "stop_conditions": [
    "需要接口或契约变更",
    "需要 allowed_paths 之外的写入",
    "必须新增受支持的参数类型"
  ]
}

{
  "issue": "136",
  "human_owner": "Quchaosheng",
  "objective": "在 wbcan 中保留标准 SocketCAN 的本地环回归属与自有消息语义。",
  "allowed_paths": [
    "kernel/wbcan/wbcan.c",
    "kernel/wbcan/test_wbcan.sh",
    "docs/task_packets/linux-wbcan-echo-semantics.json"
  ],
  "read_only_paths": ["firmware/**", "interfaces/**", "AGENTS.md"],
  "forbidden": ["robot/control/**", "firmware/**", "interfaces/**"],
  "acceptance": [
    "默认情况下，发送方不会收到自己的帧。",
    "对端套接字能收到已启用的本地环回。",
    "显式加入的自有帧携带标准确认标志。",
    "禁用 CAN_RAW_LOOPBACK 会抑制本地投递。",
    "位翻转作用于私有接收副本。",
    "分配失败恰好丢弃一份接收副本。"
  ],
  "commands": [
    "make -C kernel/wbcan",
    "make -C kernel/wbcan checkpatch",
    "sudo make -C kernel/wbcan reload",
    "sudo make -C kernel/wbcan test"
  ],
  "evidence": ["模块构建输出。", "checkpatch 输出。", "特权自有消息运行时断言。"],
  "stop_conditions": [
    "需要修改固件或接口。",
    "目标内核缺少标准 SocketCAN 回显辅助函数。",
    "特权运行时套件无法执行。"
  ]
}

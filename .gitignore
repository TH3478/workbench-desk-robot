__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.pytest_cache/
build/
dist/
*.db
*.sqlite
*.bag
*.mcap
node_modules/
coverage.xml
/site/

# 仓库根目录下生成的 colcon / ament 构建产物。
/install/
/log/

# AI 工具工作目录——必须留在历史之外。
# 一旦提交，清除它们就需要重写历史并强推。
# 注意：docs/task_packets/ 刻意不被忽略。任务包是一份经过评审的契约，
# 界定 AI 任务可以触及的范围（允许路径、测试、证据、停止条件）。
# `make task-check` 以及 tools/scripts/check_task_packet.py 的默认输入都会引用它。
.claude/
.codex/
.cursor/
.aider*
.github/copilot-instructions.md
docs/superpowers/

# 内部规划：团队结构、人员配置与止损决策保留在仓库之外。
# 公开的产品证据模板位于 docs/product/ 下，不得包含私有参与者数据。

# 本地运行产物
data/raw/
data/processed/
data/reports/
runs/
*.log
